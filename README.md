# Neuron
AI训练，数据很简单，训练出来的模型效果不是很好

# 如何使用
运行train.py训练，use.py则是使用训练好的模型

你可以选择直接运行use.py使用以训练好的模型（best_model.pth），也可以选择运行train.py重新训练（已经训练好的那个模型真的不咋地）
# 需要的库
1.PyTroch    2.jieba

    pip install torch
    pip install jieba
